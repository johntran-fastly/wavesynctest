apiVersion: v1
kind: Service
metadata:
  name: app-svc
  namespace: wave-demo
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  selector:
    app: broken-app  # Depends on the Deployment's pods
  ports:
  - port: 80
    targetPort: 80
