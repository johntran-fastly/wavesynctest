apiVersion: batch/v1
kind: Job
metadata:
  name: wave-0-started
  namespace: wave-demo
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
      - name: log
        image: busybox:1.36
        command: ["sh", "-c", "echo '=== WAVE 0 STARTED at $(date) ===' && sleep 5"]
